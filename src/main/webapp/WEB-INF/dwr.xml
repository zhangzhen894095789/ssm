<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE dwr PUBLIC "-//GetAhead Limited//DTD Direct Web Remoting 3.0//EN" "http://getahead.org/dwr/dwr30.dtd">
<dwr>
<!-- 与springmvc整合不需要此配置文件,而且整合springmvc可以直接使用mvc的Dsipatcher前段控制器  只需要配置里面增加 dwr url-mapping 映射即可 -->
    <allow>
    <!-- 创建并远程连接你的bean  javascript对应java推送消息的类名生成的js的名称 -->
        <create creator="spring" javascript="dwrPush">
            <param name="beanName" value="dwrPushService"></param>
            <!-- 这个标签可以写也可以不写,不写默认的所有的方法-->
            <include method="onPageLoad"/>
            <include method="sendMessageAuto"/>
            <include method="test"/>
            <!-- <auth role="" method=""/> -->
        </create>
       <!--<convert converter="javax.faces.BigDecimal" match=""/>-->
    </allow>
    
   <!--  <signatures>
    
    </signatures>
     -->
    
   <!-- creator 属性必须的 指定使用那个创建者
   		new：哪个使用Java'new'运算符。
		static：使用'getInstance（）'方法（默认情况下）来获取类的一个实例。
		无：这不会创建对象。看下面为什么。（V1.1 +）
		脚本：通过BSF使用BeanShell或Groovy等脚本语言。
		spring：通过Spring框架访问bean。
		jsf：使用JSF中的对象。（V1.1 +）
		struts：使用struts FormBeans。（V1.1 +）
		页面流：可以访问从蜂巢或Weblogic的一个页面流。（V1.1 +）
		ejb3：一个实验创建者来访问EJB3会话bean。该代码在经过更多测试之前不应被视为生产质量，并且具有官方维护者。（V2.0 +）
		
		
		JavaScript属性是必需的 - 它会在浏览器中给您新创建的对象一个名称。你应该避免使用JavaScript保留字
		
		scope属性与servlet规范定义的scope属性大致相同。它允许您指定可用的bean。选项是“应用程序”，“会话”，“请求”，“页面”和附加范围“脚本”。这些值中的前4个应该是Servlet和JSP开发人员所熟悉的。“脚本”范围允许您具有类似于绑定到页面中而不是Cookie中的ID的HTTP会话。
		范围属性是可选的。它默认为“页面” The options are "application", "session", "request", "page" and an additional scope "script".
		
		<exclude/> <include/>包含和排除元素允许创建者限制对类方法的访问。创建者应该指定包含元素的列表（这意味着默认策略是拒绝）或排除元素的列表（这意味着默认策略是允许访问）
		
	-->
	
	<!-- 自定义创建者 -->
	<!-- <init>
		<creator class="" id=""/>
	</init> -->
</dwr>